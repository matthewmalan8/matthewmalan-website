---
name: Dropshipping hours
unit: hours
---
Auto-computed: each Daily Log in /dropshipping-admin/ already records `hoursWorked` + `minutesWorked`. Those are summed automatically — no need to log entries here for hours you've already entered in the daily log.

You CAN still add manual entries here if you want to log time that didn't get a daily log entry. They'll stack with the auto-computed values.
